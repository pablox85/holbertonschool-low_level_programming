#include <stdio.h>
// corte en e
//corte en q
int main(void)
{
        char i;
        for (i = 'a'; i <= 'z'; i++) 
        if (i != 'e' && i != 'q')
        {
            putchar(i);
        }
{


        putchar('\n');
        return (0);
}
}
