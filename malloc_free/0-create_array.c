#include "main.h"
#include <stdlib.h>
/**
 *
 *
 *
 *
 */

char *create_array(unsigned int size, char c)
{
	unsigned int size;
	char c;
	char *arr;

	if (size == 0)
	{
		return (NULL);
	}

	arr = malloc(sizeof(char) * 10);







}
