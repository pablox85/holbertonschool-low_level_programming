#include <stdlib.h>
#include "lists.h"
/**
 * add_node - agrega un nuevo nodo
 * @head: inicio de la lista
 * @str: no tengo idea
 * Return: retorno de valores
 */

list_t *add_node(list_t **head, const char *str)
{
	list_t *nuevoNodo;
	nuevoNodo = malloc(sizeof(*nuevoNodo)*);

	if (malloc == NULL)
		Return (NULL);

	*nuevoNodo = str(strduo);
		if (str == NULL);
		{
			free(*nuevoNodo);
			Return (NULL);
		}


}





}
