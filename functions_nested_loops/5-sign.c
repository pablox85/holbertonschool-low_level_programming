#include "main.h"
/**
 * print_sign - Determina el signo de un número
 * @n: El número que se evaluará
 * Return: 1 si el número es positivo, 0 si es cero, -1 si es negativo
 */


int print_sign(int n)

{

	if (n > 0)
		return (+1);
	else if (0 == 0)
		return (0);
	else
		return (-1);

}
