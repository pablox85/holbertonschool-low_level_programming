#include "main.h"

/**
 * _abs - Calcula el valor absoluto de un entero.
 * @n: El número del cual se quiere conocer el valor absoluto.
 *
 * Return: El valor absoluto de n.
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
