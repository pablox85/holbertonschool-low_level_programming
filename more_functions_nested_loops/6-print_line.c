#include "main.h"
/**
 *
 *
 *
 */

void print_line(int n)
{

	int i;

	for (i = 0; i <= n; i++)
	{
		if (n > 0)
		{
			_putchar(95);
		}
		if (n <= 0)
		{
			
			_putchar(10);
			
		}
	
	}

_putchar(10);
}
